services:
  postgres:
    image: 1181.s.kuaicdn.cn:11818/docker.io/postgres:17.6
    environment:
      POSTGRES_USER: dys
      POSTGRES_PASSWORD: dys123acct?
      POSTGRES_DB: test
    ports:
      - 5432:5432
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db.sql:/docker-entrypoint-initdb.d/init-db.sql

volumes:
  postgres_data: